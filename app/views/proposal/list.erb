<div class="container">
  <h1>Lista de Propuestas</h1>

  <table class="table">
    <tr>
      <th>Título</th>
      <th>Autor</th>
    </tr>
    <% for @proposal in @proposals %>
      <tr>
        <td><%= link_to @proposal.title, url(:proposal, :detail, proposal_id: @proposal.id) %></td>
        <td><%= @proposal.author %></td>
      </tr>
    <% end %>
  </table>
</div>
