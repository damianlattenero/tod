<div class="container">
  <h1>Lista de Propuestas</h1>

  <table class="table table-bordered">
    <tr>
      <th>Título</th>
      <th>Autor</th>
    </tr>
    <% @proposals.each do |proposal| %>
      <tr>
        <td>
          <%= link_to proposal.title, url_for(:proposal, :detail, :proposal_id => proposal.id) %>
        </td>
        <td>
          <%= proposal.author %>
        </td>
      </tr>
    <% end %>
  </table>
</div>