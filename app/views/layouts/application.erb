<!DOCTYPE html>
<html lang="es-AR">
  <head>
    <meta charset="utf-8">
    <title>TOD - Organizador de conferencias</title>
    <%= stylesheet_link_tag '/assets/application' %>
  </head>
  <body>
    <div class="container">
      <div class="container page-header">
      <nav class="navbar navbar-default" role="navigation">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand">Tod</a>
          </div>
          <ul class="nav navbar-nav">
            <li><%= link_to 'Inicio', url_for(:home, :index) %></li>
          </ul>
          <ul class="nav navbar-nav">
            <li><%= link_to 'Nueva Propuesta', url_for(:proposal, :new) %></li>
          </ul>
          <form class="navbar-form navbar-right" role="search">
            <div class="form-group">
              <input type="text" class="form-control" placeholder="Búsqueda">
            </div>
            <button type="submit" class="btn btn-default">Buscar</button>
          </form>
        </div>
      </nav>
      </div>

      <div class="container">
        <div>
          <%= [:error, :warning, :success, :notice].map { 
            |type| flash_tag(
              type, :class => "alert alert-#{type} fade in", :bootstrap => true
            )}.join.html_safe 
          %>
        </div>

        <%= yield %>

      </div>
      <!-- footer -->
      <div class="footer">
        <hr/>
        <p> Tod | 2014 </p>
      </div>

    </div>

    <%= javascript_include_tag '/assets/application' %>
  </body>
</html>
