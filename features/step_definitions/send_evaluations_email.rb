And(/^a proposal "([^"]*)" with (\d+) revisions$/) do |arg1, arg2|
  pending
end

When(/^an admin user visits proposal "([^"]*)" details$/) do |arg|
  pending
end

And(/^the proposal does have (\d+) revisions$/) do |arg|
  pending
end

Then(/^should see the "([^"]*)" button$/) do |arg|
  pending
end

When(/^clicks on  "([^"]*)" button$/) do |arg|
  pending
end

Then(/^it should display  "([^"]*)"$/) do |arg|
  pending
end